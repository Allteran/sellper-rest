var loginPage = new Vue({
    el: '#loginPage',
    template:
        '<body class="text-center">' +
            '<form class="form-signin" action="/login" method="get"></form>' +
        ' </body>'
});